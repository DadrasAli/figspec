# The absolute minimum: a 'series' list and an 'output_basename'.
# Every other key (title, colors, grid, figsize, ...) takes its default.
series:
  - csv: "csvs/client_a.csv"
    column: "acc"
    label: "Client A accuracy"
output_basename: "out/01_minimal"
